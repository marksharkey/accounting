# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=precisionpros
DB_USER=ppros
DB_PASSWORD=

# JWT Auth — generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# App
APP_NAME=PrecisionPros Billing
APP_VERSION=1.0.0
DEBUG=True

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=PrecisionPros Billing
SMTP_FROM_EMAIL=

# Invoice Settings
INVOICE_PREFIX=PP
CREDIT_MEMO_PREFIX=PP-CM
INVOICE_DUE_DAYS=12
INVOICE_SEND_DAY=20
LATE_FEE_DAY=10
SUSPENSION_DAY=20
